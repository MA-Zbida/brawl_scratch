"""Figure generation for the technical report.

Every figure in reports/ is produced by a script in this package, from run CSVs
written during training. No figure is drawn by hand, so the paper regenerates
end to end from a run directory.
"""
